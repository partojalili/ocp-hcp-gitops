apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nodejs-app-build

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  
  - route.yaml
  
  
  - pipeline/pipeline.yaml
  - pipeline/eventlistener.yaml
  

commonLabels:
  app: nodejs-app-build
  app.kubernetes.io/name: nodejs-app-build
  app.kubernetes.io/component: backend
  app.kubernetes.io/part-of: nodejs-app-build
  app.kubernetes.io/managed-by: argocd
