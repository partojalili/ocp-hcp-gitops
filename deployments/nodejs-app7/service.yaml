apiVersion: v1
kind: Service
metadata:
  name: nodejs-app7
  namespace: nodejs-app7
  labels:
    app: nodejs-app7
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: nodejs-app7
