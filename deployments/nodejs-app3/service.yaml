apiVersion: v1
kind: Service
metadata:
  name: nodejs-app3
  namespace: nodejs-app
  labels:
    app: nodejs-app3
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: nodejs-app3
