apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: nodejs-app3
  namespace: nodejs-app
  labels:
    app: nodejs-app3
spec:
  to:
    kind: Service
    name: nodejs-app3
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
