apiVersion: v1
kind: Service
metadata:
  name: nodejs-app6
  namespace: nodejs-app6
  labels:
    app: nodejs-app6
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: nodejs-app6
