apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: nodejs-app6
  namespace: openshift-gitops
  labels:
    app: nodejs-app6
spec:
  generators:
    - clusterDecisionResource:
        configMapRef: acm-placement
        labelSelector:
          matchLabels:
            cluster.open-cluster-management.io/placement: nodejs-app6-placement
        requeueAfterSeconds: 180
  template:
    metadata:
      name: nodejs-app6
      labels:
        app: nodejs-app6
    spec:
      project: default
      source:
        repoURL: https://github.com/partojalili/ocp-hcp-gitops.git
        targetRevision: main
        path: deployments/nodejs-app6
      destination:
        name: '{{`{{name}}`}}'
        namespace: nodejs-app6
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
---
apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: nodejs-app6-placement
  namespace: openshift-gitops
spec:
  predicates:
    - requiredClusterSelector:
        labelSelector:
          matchLabels:
            name: hcp-cluster
