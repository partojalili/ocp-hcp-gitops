apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: network-policies-placement
  namespace: openshift-gitops
spec:
  predicates:
    - requiredClusterSelector:
        labelSelector:
          matchLabels:
            environment: production
