apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: clusters-hcp1

resources:
  - ../../base

patchesStrategicMerge:
  - hostedcluster-patch.yaml

commonLabels:
  environment: production
