apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: devhub4-ssh-key
  namespace: clusters-devhub4
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: hcp-secrets-store
    kind: ClusterSecretStore
  target:
    name: devhub4-ssh-key
    creationPolicy: Owner
  data:
    - secretKey: id_rsa.pub
      remoteRef:
        key: ocp-ssh-key
        property: ssh-publickey
