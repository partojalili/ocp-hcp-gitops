apiVersion: hive.openshift.io/v1
kind: ClusterDeployment
metadata:
  name: gcp-cluster
  namespace: gcp-cluster
  labels:
    cluster-name: gcp-cluster
    platform: gcp
    vendor: OpenShift
    cloud: GCP
    cluster.open-cluster-management.io/clusterset: global
spec:
  baseDomain: 86nk6.gcp.redhatworkshops.io
  clusterName: gcp-cluster

  # Control plane configuration
  controlPlaneConfig:
    servingCertificates: {}

  # Install platform
  platform:
    gcp:
      credentialsSecretRef:
        name: gcp-cluster-gcp-creds
      region: us-east1

  # Provisioning details
  provisioning:
    installConfigSecretRef:
      name: gcp-cluster-install-config
    sshPrivateKeySecretRef:
      name: gcp-cluster-ssh-private-key
    imageSetRef:
      name: img4.20.24-x86-64-appsub

  # Pull secret
  pullSecretRef:
    name: gcp-cluster-pull-secret
