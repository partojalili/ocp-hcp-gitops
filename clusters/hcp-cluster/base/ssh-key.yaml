apiVersion: v1
kind: Secret
metadata:
  name: hcp-cluster-ssh-key
  namespace: clusters-hcp-cluster
type: Opaque
stringData:
  id_rsa.pub: |
    # SSH public key will be auto-generated by HyperShift if not provided
    # Users can update this after cluster creation if needed
